# Конспект №13 Условная оптимизация. Часть 2.
## Задача условной оптимизации

## Метод окаймленного Гессиана

## Решение задачи условной оптимизации
Условная задача оптимизации:

$$\begin{cases} 
  f(x_1, x_2,..., x_n) \to \min\limits_{x_1,x_2,…,x_n} \\ 
  f_1(x_1, x_2,..., x_n) = 0 \\ 
  … \\ 
  f_p(x_1, x_2,..., x_n) = 0  
\end{cases}$$

Имеем $n$ переменных , $p$ уравнений-условий

Если у нас $𝑝$ уравнений-условий, то:

- Если угловые миноры $𝐻_{2𝑝+1},⋯,𝐻_{𝑝+n}$ положительны для чётного 𝑝 или отрицательны для нечётного 𝑝, то это точка условного минимума
- Если знаки угловых миноров $𝐻_{2𝑝+1},⋯,𝐻_{𝑝+n}$ чередуются и начинаются с минуса для чётного 𝑝 или с плюса для нечётного 𝑝, то это точка условного максимума
- Если знаки не совпадают, то это не точка условного минимума или максимума
- Если где-то угловой минор нулевой, то нужны дополнительные данные

## Общий алгоритм условной оптимизации

1. Имеем систему:

$$\begin{cases}
f(x,y) \to \min\limits_{x,y} \\
f_1(x,y) = 0
\end{cases}$$

2. Выписываем функцию Лагранжа:

$L(x, y, \lambda) = f(x,y) + \lambda f_1(x,y)$

3. Вычисляем градиент по всем переменным:\
$\nabla_x (x,y, \lambda)$ \
$\nabla_y (x,y, \lambda)$ \
$\nabla_{\lambda} (x,y, \lambda)$

4. Приравниваем градиент к нулю:

$$\begin{cases}
  \nabla_x (x,y, \lambda) = 0 \\ 
  \nabla_y (x,y, \lambda) = 0 \\ 
  \nabla_{\lambda} (x,y, \lambda) = 0
\end{cases}$$

5. Решаем систему уравнений и находим критические точки: \
$M_1, M_2, …$

6. Строим гессиан и проверяем критические точки \
$H_3$ — ? (положителен/отрицателен/не имеет знака)

## Полный конспект
https://abrupt-moose-9e7.notion.site/13-2-c2406570ef884c578fa7312dfbbb3be2
